item = {
    "title": "Bread",
    "price": 1.6,
    "quantity": 4,
}

amount = item["price"] * item["quantity"]
item["amount"] = item["price"] * item["quantity"] # Solution from Codedamn lesson Dictionaries
# print(amount)
# item.update({"amount": amount})
print(item)